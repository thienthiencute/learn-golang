package internal

import (
	"github.com/teoit/gosctx"
)

func RoutesPubsub(serviceCtx gosctx.ServiceContext) {
	// Setup pubsub routes or subscribers here
	// e.g. users.SetupPubsubUser(serviceCtx)
}
